#ifndef HEADER_H
#define HEADER_H
void randomMATRIX(int ** matrix, int len);
void writeMATRIX(int ** matrix, int len);
void randomRESULT(int * tab, int len);
void finishApp();
void calculate(int ** matrix, int len);
void writeMATRIX(int ** matrix, int * res, int len);
void startApp();

#endif // HEADER_H
